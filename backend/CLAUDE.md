# CLAUDE.md

@../AGENTS.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Read `../DESIGN.md` before making architectural decisions here.** It records the rewrite's design decisions and open questions (auth strategy, API shape, what's deliberately deferred vs. why) — several sections of this file describe a first-pass implementation that `DESIGN.md` has already superseded (notably: no-auth-yet and the SvelteKit-side response reshaping). Don't treat this file's description of the current code as the target shape without checking there first.

**Keep `../DESIGN.md` up to date whenever you make or discover something that belongs in it** — a new architectural decision, a decision that gets superseded, or a stale claim in it that turns out to be wrong (e.g. reasoning that names a specific condition for revisiting something, where that condition has since become true but nobody went back and did it — this has already happened once, see the `canEdit`/`canDelete` correction in DESIGN.md's API shape section). Update it in the same pass as the code change, not as a follow-up someone might skip. Mark superseded text as superseded rather than silently deleting it — DESIGN.md's own stated convention, and the only way the next reader (human or agent) can tell a decision changed instead of assuming this file just drifted out of sync.

## Status

This Go module is becoming the backend for the [Dsek-LTH/web](https://github.com/Dsek-LTH/web) site (the main SvelteKit app lives in the parent directory, one level up), replacing it piece by piece. Articles (`internal/articles`, `internal/api/huma_articles.go`) was the first ported feature, and **the SvelteKit app actually calls this API now** for it (see "Consumed by SvelteKit" below) — this isn't just a parallel implementation, it's load-bearing. Events (`internal/events`, `internal/api/huma_events.go`) is the second — ported server-side (CRUD, recurring series, going/interested, comments, tags) per `DESIGN.md`'s "Events" section, and **the SvelteKit frontend calls it now too**, for the subset that has a real backing endpoint (list/detail/create/edit/delete/going-interested/comments) - see "Events routes" below for exactly which routes still stay on Prisma and why. Directory (`internal/members`, `internal/committees`, `internal/accesspolicies`) is the third, per `DESIGN.md`'s roadmap "Phase 1" — see "Directory routes" below. Nollning (`internal/nollning`) is the fourth, per that same roadmap's "Phase 2" — a real `nollning_seasons` table plus one service every other domain calls into for phase/season/staben/phadder-role questions, replacing three previously-independent ad-hoc mechanisms (see `DESIGN.md`'s "Nollning: proposed redesign"); **backend and frontend both**. Phase 3 ("Simple standalone CRUD" — songbook, alerts, info pages, governing documents, medals; `internal/songs`, `internal/alerts`, `internal/markdown`, `internal/governingdocs`, `internal/medals`) is the fifth — **backend and frontend both, fully wired and verified against the dev DB** — see "Songbook routes", "Alert routes", "Info-pages routes", "Governing-documents routes", and "Medals routes" below. Shop/tickets/bookings and everything else in the SvelteKit backend still live only in `../src`, with shop/tickets specifically decided to never be ported at all (see `DESIGN.md`'s "Shop / tickets: cut from scope entirely"). The MinIO-backed `/documents`, `/documents/requirements`, `/documents/upload` file-browsing routes are unrelated to governing documents despite the shared URL prefix — no Prisma model at all, deferred to a future phase's real `Uploader` extension (see `DESIGN.md`'s Phase 3 section for why these are two different things). Data access is pgx + sqlc (see below); HTTP routing is the stdlib `net/http` `ServeMux` (see API section); DB migrations go through `golang-migrate` (see "Database migrations" below) — all were open questions when this file was first written and are now settled precedents for whatever gets ported next.

Runs on **`:8090`**, not `:8080` — the SvelteKit app's `scheduler-service` already claims `:8080` in local dev (`SCHEDULER_ENDPOINT` in `../.env`). See `main.go`.

## Commands

- Build: `go build ./...`
- Run: `go run .`
- Dev (hot reload): `make dev` (runs `go tool air`; no `.air.toml` yet, uses air's defaults, output in `./tmp` which is gitignored)
- Format: `make format` (runs `goimports` then `golines` w/ `gofumpt` as the base formatter — don't use plain `gofmt`, this is the project's real formatting step)
- Regenerate DB code after editing `internal/db/schema.sql` or `internal/db/queries/*.sql`: `make generate` (runs `go tool sqlc generate`, config in `sqlc.yaml`)
- Apply/inspect DB migrations: `go tool migrate up` / `go tool migrate down [n]` / `go tool migrate force <version>` / `go tool migrate version` — see "Database migrations" below. Run from `backend/` like everything else here.
- Vet: `go vet ./...`
- Test (once tests exist): `go test ./...`; a single test: `go test ./... -run TestName`

Module path: `github.com/dsek-lth/web/backend`, Go 1.26.0.

`air`, `golines`, `goimports`, `gofumpt`, `sqlc`, and `cmd/migrate` (this module's own minimal migration CLI, see below — not the stock `golang-migrate/migrate/v4/cmd/migrate`) are declared as `tool` dependencies in `go.mod` (Go 1.24+ tool directive) and invoked via `go tool <name>` — not installed globally. `golines --base-formatter` is passed the string `"go tool gofumpt"` rather than the bare `gofumpt` binary name, since `gofumpt` isn't on `$PATH`; keep that in mind if the Makefile is ever touched.

## Database

Data access is **pgx + sqlc**, chosen to replace `../src/database/schema.zmodel` (ZenStack/Prisma). Not an ORM: `internal/db/schema.sql` declares table shapes, `internal/db/queries/*.sql` holds hand-written SQL, and `sqlc generate` emits typed Go (`Queries` struct, param/result structs) into `internal/db/*.go`. Treat every non-`schema.sql`/`queries/` file in `internal/db/` as generated — don't hand-edit it, edit the SQL and regenerate.

**This backend connects to the same live Postgres database the SvelteKit app uses** (`POSTGRES_URL`), not a fresh one it owns migrations for. `internal/db/schema.sql` is a *description* of tables that already exist — it is never run as a migration. Consequently:

- Prisma/ZenStack (`../src/database/`) remains the source of truth for the actual DB schema and migrations for now. If a table changes there, `internal/db/schema.sql` has to be updated by hand to match — nothing keeps them in sync automatically.
- Only the tables articles need were ported so far (articles, authors, members, committees, positions, mandates, phadder_groups, custom_authors, custom_author_roles, tags, article_comments, article_requests, plus the two implicit join tables `_article_tags`/`_article_likes`). Extend `schema.sql` with more tables as more features get ported — don't feel obligated to port everything at once.
- `schema.sql` was written by cross-checking `\d <table>` against the live dev DB, not just transcribed from `schema.prisma` — several columns actually differ from what the Prisma file implies (e.g. `mandates."phadderInId"` and `phadder_groups."createdAt"` are camelCase with no snake_case mapping; `authors.type` is a generated/stored column). If a discrepancy shows up again, trust `psql \d`, not `schema.prisma`.
- **Column order in `schema.sql` must match the live table.** sqlc resolves `SELECT *` / `RETURNING *` positionally against the schema — if `schema.sql`'s column order drifts from the real table, generated code silently scans values into the wrong struct fields. The queries in `internal/db/queries/` avoid this by never using `*`; keep doing that in new queries rather than re-verifying column order every time.
- Prisma connection strings carry a `?schema=public` query param that pgx doesn't understand (rejected as an unrecognized server parameter). `internal/db/pool.go`'s `NewPool` rewrites it to the real Postgres GUC, `search_path`, before connecting — this is why `POSTGRES_URL` from the root `.env` works unmodified.
- `main.go` loads `backend/.env` if present, else falls back to `../.env` (the SvelteKit app's env file) — see the comment there for why `godotenv.Load` needs two separate calls rather than one call with two filenames.
- Local dev Postgres is the `dsek-db` container from `../docker-compose.yml` (`docker start dsek-db` / `docker compose up -d db` from the repo root).

### Database migrations

`internal/db/migrations/` is a real `golang-migrate` migration directory (plain versioned `.sql` files, `<timestamp>_<description>.up.sql`), applied via `go tool migrate` (`cmd/migrate/main.go` — a minimal wrapper around the `golang-migrate` library, not the stock CLI binary; see that file's doc comment for why: the stock binary selects DB/source drivers via build tags, and `go tool` has no way to pass build tags through). See `DESIGN.md`'s "DB migrations, once Prisma is gone" section for the full decision, but the practical points:

- The directory currently holds a **copy** of Prisma's entire migration history (`../src/database/prisma/migrations/*/migration.sql`, copied not moved — Prisma is still the live, active migration path for every table Go hasn't ported, so its own directory has to stay intact and operative). The two histories are expected to diverge from here: Prisma keeps gaining migrations for tables Go doesn't own, Go gains migrations only for tables it does.
- The live dev DB already has all of Prisma's migrations applied, so `migrate`'s own tracking table (`schema_migrations`, distinct from and non-conflicting with Prisma's `_prisma_migrations`) was bootstrapped via `force`, not replayed.
- **`sqlc` still reads from the hand-maintained `internal/db/schema.sql`, not from this migrations directory** — tried pointing it at `internal/db/migrations` instead (same "single source of truth" reasoning as adopting `migrate` in the first place) and reverted after testing: `sqlc` has no way to model only a subset of tables, so pointed at the full migration history it generated a Go struct for *every* table in the live DB, including the shop/ticket/order/payment tables this project has decided not to model in Go at all (see `DESIGN.md`'s "Shop / tickets: cut from scope entirely"). `schema.sql`'s existing hand-sync-against-`psql \d` workflow (see below) is unchanged by any of this.
- New migrations for tables Go owns should include a real `.down.sql` alongside the `.up.sql` — the migrations copied from Prisma don't have one (Prisma never generated down migrations), but that's a property of the old history, not a pattern to continue.

## API

`internal/api` wires handlers onto `net/http`'s built-in `ServeMux` via **huma** (`github.com/danielgtaylor/huma/v2`, `humago` adapter) — code-first: handlers take/return typed Go structs and huma derives the OpenAPI spec (served at `/openapi.json`, docs at `/docs`) from those types at request-routing time, so the spec can't drift from the code. See DESIGN.md's "API shape and frontend integration" for why. `internal/articles`/`internal/events` are the domain/service layers (`Service`, wrapping `*db.Queries`); `internal/api` only does HTTP concerns (typed request/response structs, error-code mapping via `huma_errors.go`) and never touches `internal/db` directly. Every request goes through `auth.Middleware` (see below) and `locale.Middleware` before reaching a handler, plus a permissive CORS layer since the SvelteKit frontend calls this API directly from the browser (see DESIGN.md).

**huma gotcha: two domain packages cannot define same-named DTO structs.** huma's OpenAPI schema registry names a component schema after the bare Go type name, not the package-qualified one - registering routes from two packages that each define their own `Member`/`Tag`/`Comment` struct **panics at server startup** ("duplicate name"), not at `go build`/`go vet` time. `internal/apitypes` holds the shared shapes (`Member`, `Tag`, `Comment` - identical across articles/events since they describe the same `members`/`tags` tables); each domain package references them via a type alias (`type Member = apitypes.Member`), not its own struct, so existing call sites don't change and huma sees one shared type. Check any new domain package's exported DTO names against every other registered package before assuming `go build` passing means the server will actually start.

### Articles routes

Routes (all under no path prefix — `main.go` mounts the router at `/`; registered in `huma_articles.go` and `huma_directory.go`):

- `GET /articles` — list, query params `search`, repeated `tags` (tag UUIDs, ANY-match), `committeeId`, `authorStudentId`, `page` (1-based), `pageSize`. Always published+non-removed only — there's no unfiltered list endpoint, only the single-article one below.
- `GET /articles/{slug}` — detail, published+non-removed only (404 otherwise), unless `?status=any` is passed, which bypasses that filter (for an author loading their own draft/scheduled article — see `Service.GetAny`).
- `POST /articles`, `PATCH /articles/{slug}` — create/update. **Update is full-replace (PUT semantics dressed as PATCH), not a partial patch** — the caller must resend every field, including `publishedAt`; omitting it un-publishes the article. This was a deliberate simplification, not an oversight — revisit only if a real partial-update caller shows up. The author's `member` is always resolved from the acting identity (see Auth below), never from the request body — there's no way to save an article under a different member's byline than whoever is authenticated.
- `PATCH /articles/{slug}/schedule` (body `{"scheduledId"}`) — targeted single-field write for the caller's external scheduler task id, set after scheduling a future publish succeeds elsewhere (this API doesn't schedule anything itself, see Mocking section). Deliberately separate from the full-replace update above.
- `DELETE /articles/{slug}` — soft-delete (`removed_at`), row is not removed
- `POST /articles/{slug}/likes`, `DELETE /articles/{slug}/likes` — as the acting identity, no memberId in the request
- `POST /articles/{slug}/comments` (body `{"content"}`), `DELETE /articles/{slug}/comments/{commentId}` — as the acting identity
- `GET /tags`
- `POST /uploads` (multipart, field `file`) — see Mocking section; storage is mocked, returns a placeholder URL
- `GET /committees` (optional `?shortName=`), `GET /members/{id}/mandates` (currently-active only), `GET /custom-authors` (unfiltered — see doc comment on `ListCustomAuthors` query) — small "directory" endpoints added to support the article author-picker and committee-news page, not a first step toward porting those domains in full.

Ported from the SvelteKit backend (`src/lib/news/*`, `src/routes/(app)/news/*`): listing with search/tag/committee/author filters/pagination, publish-window + soft-delete filtering, slug generation (`internal/slug` — byte-for-byte port of `src/lib/utils/slugify.ts`, shared with events, tested against its test cases in `internal/slug/slug_test.go`), author resolution (member / mandate / custom-author, reused via `FindAuthor`-or-`CreateAuthor` like the original), tags, comments, likes, and HTML sanitization of `bodySv`/`bodyEn` (via `bluemonday.UGCPolicy()`, replacing `isomorphic-dompurify`).

**Still genuinely not ported** (no interface exists because nothing depends on it yet):
- **The article moderation/request workflow** (`article_requests` table, `ArticleRequest` in Prisma) — schema exists (ported as an articles dependency), no endpoints.
- The nollning-specific tag-prefix filtering in `BASIC_ARTICLE_FILTER` (`NOLLNING_TAG_PREFIX`) — not replicated since the nollning feature isn't ported. SvelteKit's tag *listing* (as opposed to article listing) still goes through Prisma for this reason - see below.

### Events routes

Registered in `huma_events.go`. See `DESIGN.md`'s "Events" section for the full rationale behind every divergence noted below - this is just the route list.

- `GET /events` — list, query params `search`, repeated `tags` (tag UUIDs, ANY-match), `past` (bool — upcoming by default, ended events if true), `page` (1-based), `pageSize`. Visibility: non-removed only, same as articles' publish/removed filtering.
- `GET /events/{slug}` — detail, non-removed only (404 otherwise) unless `?status=any` (same bypass convention as articles, for an editor viewing a soft-removed event — see `Service.GetAny`).
- `POST /events` — create. Body is `events.EventInput`; set `recurring` (`{type, separationCount, endAt}`) to create a whole weekly/monthly/etc. series instead of a single event — every occurrence is materialized as its own row up front, not expanded at read time.
- `PATCH /events/{slug}` (query `?scope=THIS|FUTURE|ALL`, default `THIS`) — full-replace update, same PUT-dressed-as-PATCH convention as articles. `FUTURE`/`ALL` apply the new content to every affected sibling in a recurring series but preserve each occurrence's own date, only shifting the submitted start/end time-of-day (`Service.retimeOccurrence`). Unlike articles, **the acting identity never replaces `author_id`** on update — see DESIGN.md for why events deliberately diverges from articles' "always re-attribute to the editor" rule here.
- `DELETE /events/{slug}` (query `?scope=THIS|FUTURE|ALL`, default `THIS`) — soft-delete (`removed_at`); `FUTURE`/`ALL` soft-delete every affected sibling. No "own event" bypass, unlike update — only `apinames.EventDelete`.
- `PATCH /events/{slug}/attendance` (body `{"status": "going"|"interested"|"none"}`) — sets the acting member's RSVP; going/interested are enforced mutually exclusive in code (the only write path to either join table), not left to caller convention.
- `POST /events/{slug}/comments` (body `{"content"}`), `DELETE /events/{slug}/comments/{commentId}` — as the acting identity, gated on `apinames.EventComment`/`EventCommentDelete` (the old TS comment-delete action had no auth check at all - fixed, not replicated).

**Wired into the SvelteKit frontend (2026-09-01)** for the ported subset: list, detail, create, edit, delete, going/interested, comments now call this API from `../src/lib/events`/`../src/routes/(app)/events` (same `+page.ts`/`+page.server.ts` split articles already uses). **Deliberately still on Prisma, not touched by this pass**: `all-events` (admin bypass listing), `calendar`, `tv`, `subscribe` (ICS feed), `id/[id]` (legacy slug-backfill redirect), and the nollning events page - none of their backing Go endpoints exist yet (see "not ported this pass" below), so converting them would mean either building new Go endpoints or leaving them broken; both out of scope for a frontend-wiring pass. `src/lib/events/getEvents.ts` (`getAllEvents`/`getEvent`, Prisma-backed) still exists and is still correct to use for exactly those deferred routes - don't delete it. Two known gaps from this pass, both accepted because nothing currently renders the pages that would need them (`/events`, `/events/[slug]`, `/events/all-events`, `/events/calendar` are `<NotImplemented />` stubs; `create`/`[slug]/edit` have no `+page.svelte` at all, only server logic - this predates the port, not introduced by it):
- Push notifications to an event's organizer on going/interested aren't ported (see below) and aren't replicated client-side either.
- The edit page can't prefill a recurring series' own settings (type/separationCount/end date) - Go's `EventDetail` only exposes `recurringParentId`, not the `RecurringEvent` row itself; `GetRecurringEvent` exists as a query (`internal/db/queries/events.sql`) but isn't wired to any route yet.

Not ported this pass (see DESIGN.md for the full "Events" section): push notifications on going/interested, the calendar range endpoint, the ICS subscribe feed, the typeahead search endpoint, the admin all-events bypass listing, nollning event visibility.

### Directory routes (members / committees / positions / mandates / access policies)

**Both the Go backend and its SvelteKit frontend wiring are implemented** (per DESIGN.md's roadmap "Phase 1: directory foundation"). Three packages: `internal/members` (`huma_members.go`), `internal/committees` (owns Committee/Position/Mandate - they're relationally nested and always queried together; `huma_committees.go`), `internal/accesspolicies` (`huma_access_policies.go`). `Committee`/`Position`/`Mandate` moved into `internal/apitypes` from their old minimal `internal/articles`-only shapes during this port (same reason as Member/Tag/Comment - see that package's doc comment) - `internal/articles` now aliases them and keeps working with fewer fields populated; `internal/members`' own richer member-profile DTO is named `MemberProfile`, not `Member`, specifically to avoid colliding with `apitypes.Member` (this is exactly the huma "duplicate schema name" trap CLAUDE.md warns about above - it panicked at server startup during this port before the rename).

`Committee`/`Position` also carry a resolved `description` field (`DescriptionEn` if the request locale is "en" and set, `DescriptionSv` otherwise) alongside the raw pair, mirroring `Name`'s existing resolution - added mid-port once the frontend wiring pass found real consumers (`about`, `committees/[shortName]/members`) reading a single resolved description string. `Mandate.member`/`Position.committee` are populated with a full sub-object (not just an ID) in exactly the contexts that need to render/link them without a second request: a member's own mandate history (`GET /members/{studentId}`) attaches each mandate's `position.committee`; a position's mandate-holder list (`GET /positions/{id}`, `GET /committees/{shortName}` positions) attaches each mandate's `member` (with `classYear`/`classProgramme` too, for study-year statistics and programme badges) - these fields on `apitypes.Member`/`Position`/`Committee` are `nil` everywhere else, additive per the same pattern as `MandateCount`/`EmailAliases`.

`internal/committees/convert.go` centralizes `Committee`/`Position`'s row-to-DTO conversion (`toCommittee`/`toPosition`, each taking a small `committeeRow`/`positionRow` adapter struct that every sqlc-generated row type gets copied into) - added after `Description`'s resolve logic had been inlined at 8 separate construction sites across `service.go` (4 `Committee` literals, 4 `Position` literals), the same duplication `internal/articles/convert.go` already avoids for its own DTOs. Any future translated field on Committee/Position should be added to `toCommittee`/`toPosition` once, not at each call site. `dbutil.ResolveNullableName` (alongside the existing `ResolveName`) handles the case where the `Sv` column is itself nullable (like `description_sv`, unlike `name_sv`) - nil in, nil out rather than resolving against an empty string; reuse it rather than writing a package-local nullable-aware wrapper the way this package originally did.

- `GET /members` (query `classYear`, `classProgramme`, both optional filters), `GET /members/{studentId}` — directory list/profile; both require an authenticated (non-anonymous) identity, matching the old SvelteKit routes' plain "must be logged in" bar. `email` on the profile response is only resolved if the caller is viewing their own profile or holds `MemberSeeEmail` - matches old TS behavior exactly.
- `PATCH /members/{studentId}` (full-replace of `{firstName,lastName,nickname,classProgramme,classYear,graduationYear,language,bio}`, bio sanitized via bluemonday), `PATCH /members/{studentId}/food-preference` (full-replace of `{foodPreference}` alone) — two endpoints, not one, mirroring the old app's two independent forms; self-or-`MemberUpdate` authorized. **Real fix, not a replicated gap**: the old TS actions had zero server-side authorization, only client-side button-hiding. `email` and `nollningGroupId` are not editable here - `nollningGroupId` has its own dedicated endpoints now (`POST`/`DELETE /nollning/groups/{id}/nollor[/{memberId}]`, see "Nollning routes" below), not a field on this one; email was never editable in the old profile-edit form either.
- `GET /committees` (descriptions/images/banner + currently-active mandate/member counts), `GET /committees/{shortName}` (`?year=`, default current year — positions active-or-with-mandates-that-year, each with its mandates and email aliases, plus about/links markdown) — public reads.
- `PATCH /committees/{shortName}` (descriptions/images/banner), `PUT /committees/{shortName}/markdown`, `PUT /committees/{shortName}/links` — three separate endpoints mirroring the old page's three independently-submitted forms; gated on `CommitteeUpdate`.
- `GET /positions`, `GET /positions/{id}` (full, unscoped mandate history - not year-scoped like the committee detail page's positions, since the position detail page groups its own history client-side by year for study-year statistics) — public reads. `PATCH /positions/{id}` — name/description/email/active/boardMember, gated on `PositionUpdate`; combines what used to be two separate TS actions into one endpoint since positions have few fields and the flags-toggle UI never had real frontend consumers.
- `POST /positions/{positionId}/mandates` (body `{memberIds[], startDate, endDate}` — one mandate row per member), `PATCH /mandates/{id}` (dates), `DELETE /mandates/{id}` — gated on `MandateCreate`/`MandateUpdate`/`MandateDelete`. Same "real fix, not replicated gap" as member update: the old mandate CRUD actions had no server-side authorization at all.
- `GET /access-policies` (`?apiName=` filter, joins member name for studentId-scoped rows), `GET /access-policies/api-names` (distinct values for the create-flow dropdown), `POST /access-policies` (`{apiName, role|studentId}` - exactly one, validated in Go and by a DB `CHECK` constraint added this pass), `DELETE /access-policies/{id}` — gated on `AccessPolicyRead`/`Create`/`Delete`.

**Committee/board position ordering** (`internal/committees/ordering.go`) is a static Go data table ported verbatim from `src/lib/utils/committee-ordering/{enums,sort}.ts` - genuine hand-curated editorial display order with no backing DB column, applied server-side so `GET /committees/{shortName}` returns pre-sorted positions.

**Deliberately not ported this pass** (see DESIGN.md's roadmap for the full reasoning): the board page and its `SEE_STABEN` staben-hiding logic (Phase 2, bundled with the nollning redesign - porting the current position-ID-prefix hack now would mean ripping it out again immediately after); avatar/picture upload (needs real file storage, Phase 4); medals, ping, door access, member notification/subscription settings (each a distinct later-phase feature); `EmailAlias` CRUD (read-only support only - confirmed zero create/update/delete UI exists anywhere in the old app, only ever read via a nested Prisma include).

**Frontend wiring (implemented)**: `src/routes/(app)/members/*` (list, profile, edit, `me` redirect), `onboarding`, `about`, `src/routes/(app)/committees/*` (layout, detail, members subpage, position detail + mandate CRUD forms), `positions`, and all three `admin/access/*` pages now call the Go API instead of Prisma - verified via `svelte-check`/`eslint` (0 errors) and live SSR + a real mutation round-trip (create/verify/delete an access policy through the actual SvelteKit form action) against the dev DB. `src/lib/utils/positions.ts`'s `positionToCommitteeMap`/`committeeToPositionMap`/`getPositionLink` **did not need changes** - Go addresses positions by their full ID directly, and the frontend's existing short-URL scheme (`/committees/{shortName}/position/{suffix}`) still works unmodified since position IDs themselves are unchanged. One small accepted gap, pre-existing rather than introduced by this pass:
- `email` isn't part of the Go member-update API and is still written via a narrow, explicitly-commented direct Prisma call from onboarding's action - real auth already sets it from Authentik claims at first login per DESIGN.md's Auth section, so this form field may already be redundant (not confirmed). `nollningGroupId`'s equivalent gap was closed in the Phase 2 nollning frontend-wiring pass - see "Nollning routes" below.
- `updateMandate`'s success flash message no longer includes the member's name in genitive case (just "the member") - Go has no "get a single mandate" lookup, and fetching the whole position again just for a display string wasn't worth the extra round-trip. `deleteMandate`'s message was already genericized the same way for the same reason.

### Nollning routes

**Go backend and SvelteKit frontend wiring both implemented 2026-09-02** (per `DESIGN.md`'s roadmap "Phase 2: nollning redesign" — the backend pass on 2026-09-01 was scoped backend-only; a follow-up pass the next day closed out the frontend side, prompted by the discovery that the backend pass's `phadder_groups.year` → `season_id` migration had left `src/database/schema.zmodel` out of sync with the live table - see "Prisma schema drift" below, this wasn't optional cleanup). One package, `internal/nollning` (`huma_nollning.go`), replaces three previously-independent mechanisms (an `AdminSetting`-backed time window, a `[NOLLNING]` tag-name-prefix convention, and a dual-path `PhadderGroup`/mandate membership model) with a real `nollning_seasons` table and one `Service` every other domain calls into — see `DESIGN.md`'s "Nollning: proposed redesign" for the full design and which of its originally-open questions got resolved during this port.

`nollning.Service` is a genuine cross-domain dependency, not just another `internal/<domain>` package: `internal/auth.RealAuthenticator` calls it (via a `StabenInjector` interface declared in `internal/auth`, not a direct `*nollning.Service` field — `internal/nollning`'s own CRUD methods use `auth.Require`/`auth.FromContext` like every other domain service, so a direct dependency in both directions would be an import cycle; the interface breaks it, satisfied structurally, no import of `internal/nollning` from `internal/auth`) for two things on every request: `InjectStabenPolicy` (grants `apinames.MemberSeeStaben` by default outside any active season, mirroring the old `hooks.server.helpers.ts` `fetchAccessPolicies` behavior exactly) and `NollaYear` (the season's year, not `time.Now().Year()`, for `auth.DerivedRoles`' `"nolla"` pseudo-role — `DerivedRoles` takes this as an explicit parameter now rather than computing it itself). `internal/committees.Service` also takes a `*nollning.Service` directly (no cycle risk the other way — `internal/nollning` never imports `internal/committees`) for `ListBoard`'s redaction.

- `GET /nollning/current` — phase (`off`/`pre_reveal`/`post_reveal`) and the active season if any; what the frontend needs to pick a theme and route the native app, replacing `REVEAL_LAUNCH_DATE`/`CUTOFF_DATE`/`APP_PREFERRED_PAGE_COOKIE`'s scattered date logic with one call.
- `GET /nollning/seasons`, `POST /nollning/seasons`, `PATCH /nollning/seasons/{id}` — admin season CRUD, gated on `NollningSeasonManage` (new policy, no old TS equivalent — the `AdminSetting` rows it replaces were gated generically). `organizingCommitteeId` defaults to whichever committee has `short_name = "nollu"` when omitted on create, but is a real per-season nullable FK, not a hardcoded constant — see `DESIGN.md`'s decision on this.
- `GET /nollning/groups` (`?seasonId=` filter, with nolla/phadder counts), `GET /nollning/groups/{id}` (full nollor/phaddrar member lists), `POST /nollning/groups`, `PATCH /nollning/groups/{id}`, `DELETE /nollning/groups/{id}` — phadder-group CRUD, gated on `NollningPhadderGroupsManage` (mirrors the old `apiNames.NOLLNING.MANAGE_PHADDER_GROUPS` exactly). Moved out from under `committees/nollu/groups/manage` into this package's own routes.
- `POST /nollning/groups/{id}/nollor` / `DELETE /nollning/groups/{id}/nollor/{memberId}` (set/clear a member's `nollning_group_id`), `POST /nollning/groups/{id}/phaddrar` / `DELETE .../phaddrar/{memberId}` (tag/untag a member's phadder/uppdrag mandate) — mirror the old `addNolla`/`removeNolla`/`addPhadder`/`removePhadder` actions exactly, including `addPhadder`'s "member must actually hold a phadder/uppdrag mandate for this group's season" check (`FindActivePhadderMandate`). **Real improvement, not a replicated gap**: the eligibility window now uses the group's actual season `nolla_start_at`/`end_at` instead of the old hardcoded Aug 1 – Oct 1 guess (`phadderMandateFilter`).
- `GET /members/{studentId}/phadder-role` — a member's `PhadderRole` (`""`/`"nolla"`/`"phadder"`) and group id for the current season, replacing `phadderMandateFilter`-based lookups like the old `api/members/phadders`.
- `GET /board` (in `internal/committees`, `huma_committees.go` — board data is fundamentally position/committee data, not nollning-specific) — every board-flagged active position with its current holder, sorted via `ordering.SortBoardPositions` (pre-built during Phase 1 but unused until now). Positions belonging to the current season's `organizing_committee_id` are omitted unless the caller holds `MemberSeeStaben`, replacing the old `position.id.startsWith("dsek.noll")` prefix hack with a real `committee_id` comparison. **Verification note**: end-to-end tested via `AUTH_MOCK` against the dev DB for the season/phase/content-FK mechanics, but the redaction branch itself was only verified by code inspection, not a live differently-privileged request — `MockAuthenticator` always grants every policy (including `MemberSeeStaben`) to its single fixed identity, so there was no way to drive an unprivileged request through mock auth without a code change.

**Content classification**: `articles`/`events` each gained a nullable `nollning_season_id` FK straight to `nollning_seasons` (direct FK, not a tag — see `DESIGN.md`'s decision), exposed as `nollningSeasonId` on `ArticleInput`/`ArticleSummary` and `EventInput`/`EventSummary`, filterable via `GET /articles?nollningSeasonId=`/`GET /events?nollningSeasonId=`. Setting or changing it to a non-null value requires `apinames.NollningContentAssociate` (one policy shared by both domains — the user confirmed this over a per-domain pair when this was implemented, resolving `DESIGN.md`'s previously-open question); leaving it unset, or an `Update` resubmitting the article/event's own current value (recall `Update` is full-replace, so every save resubmits every field), doesn't require it.

**Prisma schema drift (discovered and fixed 2026-09-02):** the backend pass's `20260901220200_phadder_groups_season_id` migration dropped `phadder_groups.year` from the live DB, but `src/database/schema.zmodel` still declared it - Prisma's default field selection includes every scalar field unless a query narrows it with `select`, so any untouched `prisma.phadderGroup.*` call with no explicit `select` (several existed) failed at runtime with "column does not exist" from the moment that migration landed until this fix. `year` was removed from `schema.zmodel`'s `PhadderGroup` model (no `seasonId` added - nothing in Prisma-land reads/writes phadder groups anymore) and `pnpm generate` re-run. This is the first real instance of `DESIGN.md`'s "DB migrations, once Prisma is gone" section's anticipated divergence ("Prisma keeps gaining migrations for tables Go doesn't own, Go gains migrations only for tables it does") - worth checking for the same class of bug (a Prisma model still declaring a column Go's migration dropped) whenever a future phase takes over migrations for another shared table.

**Frontend wiring (implemented 2026-09-02):** every old-TS nollning mechanism that had a real, reachable consumer now calls the Go endpoints above instead - the board page (`src/routes/(app)/board`), the public and admin phadder-group pages (`committees/nollu/groups` and its `manage` subpage), the member profile/edit/onboarding pages' phadder-group read/write (`members/[studentId]`, its `edit` subpage, `onboarding` - a shared `setNollningGroup` helper in `$lib/utils/member.ts` backs all three), the `(nollning)/nollning` and `(nollning)/nolla` layouts' phase checks, `admin/debug`'s diagnostic flag, and `admin/settings`' nollning-period form (now real season CRUD, with two fields - `year`, `revealAt` - the old `AdminSetting`-backed form never had). `src/lib/components/MemberCard.svelte`'s pre-existing "TEMPORARY dual-shape" pattern (documented there as anticipating exactly this) meant `MembersList.svelte` only needed the same type-loosening treatment, no new conversion logic. **Genuinely dead code deleted** (confirmed zero remaining importers by grep before removal): `src/hooks.server.helpers.ts` (its `getAccessPolicies`/`fetchAccessPolicies` were already unreachable - `hooks.server.ts` gets policies from Go's `/me` now, not this file - `InjectStabenPolicy` above is the real mechanism), `src/routes/(app)/api/nollning/+server.ts`, `src/routes/(app)/api/members/phadders/+server.ts`, `src/lib/nollning/groups/types.ts` (`phadderGroupSchema`/`phadderMandateFilter`), and `updateNollningPeriod` (the write half of `$lib/utils/adminSettings/nollning.ts` - admin/settings writes real seasons now). **Two pages were stubs with no real `+page.svelte`** (`committees/nollu/groups/manage`, `admin/settings`, plus the `(nollning)/nollning` route's own `+page.server.ts`) - their `load`/`actions` were still ported to Go (same "port server logic even without a page.svelte" precedent as events' create/edit routes), since leaving them on the now-broken Prisma `year` column would be worse than porting logic nobody can see yet.

**Accepted gaps, not closed by this pass** (each individually confirmed, not just carried over): `$lib/utils/adminSettings/nollning.ts`'s `isNollningPeriod`/`getNollningStart` (read-only) stay, since `gallery/+page.server.ts`'s staben-album date-filtering hack is still a documented Phase 4 gap per `DESIGN.md` and genuinely still calls them. The fully-Prisma legacy routes (`all-events`, `calendar`, `events/subscribe`, `events/id/[id]`, `(nollning)/nollning/events/subscribe`, `(nollning)/nollning/messages`) keep the `[NOLLNING]` tag-prefix convention (`BASIC_EVENT_FILTER`/`BASIC_ARTICLE_FILTER`) exactly as before - none of them have a Go-backed endpoint for anything yet, unrelated to nollning specifically; converting only their nollning logic would leave a confusing half-migrated state. No season picker UI was built for article/event create/edit forms - `nollningSeasonId` is sent as `null` from `$lib/news/server/actions.ts`/`$lib/events/server/actions.ts` (a real, valid value, not a bug) until one exists. The `(nollning)/` route tree's actual product UI content is unchanged, still `<NotImplemented />` stubs - deliberately out of scope, a separate later product effort per `DESIGN.md`.

### Songbook routes

**Go backend and SvelteKit frontend wiring both implemented 2026-09-02**, per `DESIGN.md`'s roadmap "Phase 3: simple standalone CRUD". `internal/songs` (`huma_songs.go`), ported from `src/routes/(app)/songbook/*`. The `songs` table pre-dates this Go port (Prisma-created, already in `internal/db/migrations`' baseline copy) — no new migration was needed, just a `schema.sql` addition.

- `GET /songs` (`search`, repeated `category` for ANY-match, `showDeleted`, `page`, `pageSize`), `GET /songs/{slug}`, `POST /songs`, `PATCH /songs/{slug}` (full-replace), `DELETE /songs/{slug}` (soft), `POST /songs/{slug}/restore` — gated on `song:create`/`song:read`/`song:update`/`song:delete` respectively. **Reads are gated too**, not public — unlike Markdown/Alert below, the old `Song` ZModel required `song:read` for every read (`@@allow("read", has(auth().policies, "song:read"))`), a real distinction preserved, not relaxed.
- `GET /songs/categories` (raw distinct list, for the create/edit forms' datalist autocomplete) and `GET /songs/categories/grouped` (id→display-name map, for the list page's filter chips) are two separate endpoints with different shapes — mirrors the old app's `getExistingCategories` (raw) vs. `groupCategories` (grouped) having genuinely different call sites and semantics, not a redundant pair. `GET /songs/melodies` is the melody equivalent of the raw one (no grouped variant needed).
- `showDeleted` on `GET /songs` is an **exclusive toggle** (only soft-deleted songs, a trash view), not a union with active songs — matches the old list page's `deletedAt: {not: null}` filter exactly. `GetSongBySlug`'s own `includeDeleted` (used by `Get`/`Update`/`Delete`) is a **union** instead (visible whether deleted or not, for a caller holding `song:delete`) — mirrors the old detail page's real behavior (bypasses the deny filter entirely for privileged viewers, not a separate "trash mode"). Both only apply for a caller holding `SongDelete` — service-level check, not a Prisma-client swap.
- `FixSongText`/`GroupCategories` (`internal/songs/text.go`) are verbatim ports of `songbook/helpers.ts`'s `fixSongText`/`groupCategories`, including the typographic-replacement ordering (`---` before `--`, `||:`/`:||` before `|:`/`:|`) and `groupCategories`' "second category in a group falls back to the group id as display name" quirk. Slug generation reuses `internal/slug.Slugify`/`SlugWithCount` verbatim (same pattern as articles/events).
- `mayWatchVideos`'s role check stays entirely frontend (`songbook/helpers.ts`) — a client-visible UI gate over data already sent to the client, not a real access boundary Go needs to enforce (the video URL was never withheld from the client either way, old app included).

**Frontend**: `songbook/+page.ts`/`[slug]/+layout.ts` (universal loads, real pages), `create`/`[slug]/edit` keep `+page.server.ts` server actions for `create`/`update` (real authoring forms, zod for instant-feedback UX only) but `delete`/`restore` converted to direct client-side Go calls (no SvelteKit action), matching `RemoveArticleDialog.svelte`'s established pattern. `songbook/helpers.ts` had `getExistingCategories`/`getExistingMelodies`/`canAccessDeletedSongs`/`fixSongText`/`groupCategories` deleted (moved server-side into Go) — only `mayWatchVideos` remains; `songbook/create/helpers.ts` (`slugifySongTitle`) deleted entirely (Go owns slug generation now).

### Alert routes

**Go backend and SvelteKit frontend wiring both implemented 2026-09-02.** `internal/alerts` (`huma_alerts.go`), ported from `src/routes/(app)/admin/alerts` and the old `api/closeAlert` endpoint. `alerts` + the implicit join table `_alerts_closed_by` pre-date this Go port (Prisma-created) — `schema.sql` additions only, no new migration.

- `GET /alerts` — public read (matches the old `@@allow("read", true)`), each alert resolved with `message` (locale-resolved, alongside the raw `messageSv`/`messageEn` pair — same pattern as articles' `header`) and `closedByMe` (a bool resolved server-side from the acting identity, `false` for anonymous callers) instead of shipping the full closer-member-id list to the client — data minimization, matching articles' like-count-not-likers-list precedent. **This incidentally fixes a real display bug**: the old admin page's `+page.svelte` read `alert.message`, a field that never actually existed on the raw Prisma row (only `messageSv`/`messageEn` did) — Go's resolved field makes that reference correct for the first time.
- `POST /alerts`, `DELETE /alerts/{id}` (soft) — gated on `AlertManage` (`"alert"`, a single flat policy — matches the old app's non-CRUD `apiNames.ALERT` exactly, deliberately not split into create/update/delete sub-policies).
- `POST /alerts/{id}/close` — requires only a non-anonymous identity, no `AlertManage` — matches the old `api/closeAlert` endpoint's real behavior exactly (it had no `authorize()` call at all; any logged-in member could dismiss any alert for themselves — replicated as-is, not tightened or loosened).

**Frontend**: `admin/alerts/+page.server.ts` keeps `create`/`delete` as server actions calling Go. The root `(app)/+layout.server.ts`'s alert fetch (previously `prisma.alert.findMany` plus a module-level `alertsCache` — shared, mutable, process-wide state with no real TTL) now calls Go per-request instead, with no cache; `Alert.svelte`'s dismiss button and the old `/api/closeAlert` `+server.ts` (confirmed zero other consumers, deleted) both replaced by a direct client call to `POST /alerts/{id}/close`.

### Info-pages routes (Markdown)

**Go backend and SvelteKit frontend wiring both implemented 2026-09-02.** `internal/markdown` (`huma_markdown.go`), ported from `src/routes/(app)/info/[slug]` and `admin/info`. **Naming note**: DESIGN.md's Phase 3 roadmap bullet said "Markdown/governing documents", but `Markdown` actually backs these generic `/info` CMS pages, not governing documents (styrdokument) — see "Governing-documents routes" below for that separate, `Document`-backed feature. The `markdowns` table already existed in `schema.sql` (added during Phase 1 for `internal/committees`' own private reuse of it for each committee's about/links text, keyed by `{shortName}`/`{shortName}_links`) — this package is the general-purpose version the old `/info` routes used, keyed by arbitrary slugs; no new migration, no relation to committees' usage beyond sharing the physical table.

- `GET /info/{slug}` — public read (matches `@@allow("read", true)`), 404 if missing. Response includes `canEdit` (computed server-side from the exact check `Update` enforces — the frontend reads this instead of calling `isAuthorized` with a dynamically-built policy string client-side, which the old page did and which DESIGN.md's Principle #5 rules out). **Also fixes a real bug**: the old detail page read `data.markdown?.markdown`, a field that never existed on the raw Prisma row (only `markdownSv`/`markdownEn`) — Go's resolved `markdown` field makes it correct.
- `POST /info/{slug}` (create), `PATCH /info/{slug}` (update) — **unified auth**, collapsing the old app's two divergent policy groups/create paths (`MARKDOWNS.CREATE`+`MARKDOWNS.PAGE(slug).UPDATE` used by `info/[slug]/edit`, vs. a separate `MARKDOWN.CREATE` used only by `admin/info`, with no ACL auto-grant) into one: `apinames.MarkdownCreate`/`MarkdownUpdate` (`markdown:create`/`markdown:update`) as the base policies, plus a per-page dynamic ACL string (`fmt.Sprintf("markdowns:%s:update", slug)`, plural — matches the live table/old TS naming) auto-granted to the creating member via a direct `api_access_policies` insert on every create, from whichever endpoint. `Update` accepts either the base policy or the caller's own page-scoped grant (mirrors articles'/events' "own resource or policy" bypass, with a dynamic string instead of an author-id comparison). **This is a real policy-string rename** — any dev-DB `AccessPolicy` rows granting the old `MARKDOWNS.PAGE(x).UPDATE`/`MARKDOWN.CREATE` strings don't carry over automatically; regrant if needed, no compat shim was written.
- Dynamic per-instance policy strings work with `auth.Require`/`Identity.Has` exactly like static `apinames` consts — `api_access_policies.api_name` is an unconstrained `VARCHAR`, and `auth.Require` takes a plain `string`, no enum involved (confirmed by reading `internal/auth`/`internal/apinames` before implementing this — see the git history around this section for the full research). **Mock-auth caveat**: `MockAuthenticator`'s identity uses `apinames.All()`, which never includes a dynamically-granted string, so the per-page ACL bypass path can't be exercised live under `AUTH_MOCK` — verified by code inspection plus the base-policy path (which the mock does cover), same caveat already recorded for nollning's `GET /board` redaction branch.

**Frontend**: `info/[slug]/+page.ts` (converted from `+page.server.ts`, real page). `info/[slug]/edit/+page.server.ts` and `admin/info/+page.server.ts` (both stub `.svelte`, stay stubs) both call the same unified `POST`/`PATCH /info/{slug}` now — `admin/info`'s create path previously had no ACL auto-grant at all, now it does, same as `info/[slug]/edit`'s. `$lib/news/markdown/mutations.server.ts`'s `updateMarkdown` (used by the unrelated `committees/cafe` page) was **not** touched or deleted — confirmed it still has a real caller before assuming it was dead.

### Governing-documents routes

**Go backend and SvelteKit frontend wiring both implemented 2026-09-02.** `internal/governingdocs` (`huma_governingdocs.go`), ported from `src/routes/(app)/documents/governing/*` — the actual "governing documents" (styrdokument: policies/guidelines/plans-of-operation/etc.) feature, backed by the `Document` Prisma model (not `Markdown` — see "Info-pages routes" above for that naming correction). Package named `governingdocs`, not `documents`, to avoid colliding with the unrelated MinIO-backed `/documents`, `/documents/requirements`, `/documents/upload` file-browsing feature (no Prisma model at all, deferred to a future real-`Uploader` phase). `documents` table + `document_type` enum pre-date this Go port (Prisma-created) — `schema.sql` additions only, no new migration. `url` is a plain string field the author pastes in; this feature has no file-storage dependency at all.

- `GET /governing-documents` (all non-deleted, any type — year/type bucketing stays a frontend concern via `getYearOrThrowSvelteError`, same as before), `GET /governing-documents/{id}` (constrained to `type IN (POLICY, GUIDELINE)`, matching the old edit-lookup query — the other enum values have no create/edit UI anywhere), `POST /governing-documents`, `PATCH /governing-documents/{id}` — all four require `apinames.GoverningDocumentRead`/`GoverningDocumentWrite` (the latter shared across create/update/delete, matching the old app's single-policy shape exactly). **Real, necessary fix, not a replicated gap**: the old app's `new`/`[id]/edit` create/update actions had *no* explicit `authorize()` call at all, relying purely on ZenStack's model-level `@@allow` policy — Go has no such fallback layer, so these checks are newly explicit here.
- `DELETE /governing-documents/{id}` — **a real soft-delete now** (sets `deleted_at`), fixing a genuine schema/behavior mismatch in the old app: the `deletedAt` column existed but the delete action did a hard `prisma.document.delete` regardless, leaving the column effectively dead. Uses an **unconstrained-by-type** existence lookup (`GetAnyGoverningDocumentByID`, distinct from the `POLICY`/`GUIDELINE`-only `GetGoverningDocumentByID` used by `Get`/`Update`) — the list page's delete action removes any document type, matching the old `prisma.document.delete({where:{id}})`'s lack of a type filter.

**Frontend**: `documents/governing/+page.ts` (converted from `+page.server.ts`, real page). `new/+page.server.ts`/`[id]/edit/+page.server.ts` (both stub `.svelte`, stay stubs) call the new Go endpoints. The list page's delete action converted from the shared `DeleteFileForm.svelte` (a SvelteKit-action-based component, not reusable here since there's no more `+page.server.ts` action to post to) to an inline direct-client-call `AlertDialog`, matching `RemoveArticleDialog.svelte`'s pattern; `DeleteFileForm.svelte` itself is untouched (still used by the MinIO-backed sibling routes). The list page's `isAuthorized(apiNames.GOVERNING_DOCUMENT.*, ...)` calls were replaced with a plain `data.user?.policies?.includes(...)` read (matching Song's/Alert's own list-page precedent) — a flat global-policy visibility check, not the per-resource authorization *computation* DESIGN.md's Principle #5 actually rules out.

### Medals routes

**Go backend and SvelteKit frontend wiring both implemented 2026-09-02.** `internal/medals` (`huma_medals.go`) + `internal/semesters` (a verbatim port of `src/lib/utils/semesters.ts`'s academic-semester encoding — `2*year` for spring/"VT", `2*year+1` for fall/"HT"), ported from `src/lib/server/medals/medals.ts` and `src/routes/(app)/medals/*`. Read-only, no dedicated table — everything derived from `mandates`/`positions`/`committees`, already in `schema.sql`; a new `internal/db/queries/medals.sql` adds the joined/filtered queries this needed (mandate history with `board_member`/`committee_id`, candidate members active during a date window, committees excluding the fixed `valb`/`other`/`dchip`/`medalj` short-name set).

- `GET /medals` (`?semester=`, e.g. `"VT 2026"`/`"HT 2026"`, defaults to the current semester) — every medal and its recipients as of that semester. `GET /medals/download-csv` is a **plain non-huma endpoint** (like `/me` and `/auth/*`), mounted directly on the mux — CSV doesn't fit huma's typed-JSON-body model, same reasoning `POST /uploads` already established for stepping outside huma when the shape doesn't fit. Both still run behind the same auth/locale middleware chain as every huma route.
- Auth is **"must be authenticated" only, replicated exactly as-is, not tightened** — the old app's `medals/+page.server.ts`/`download-csv` had no medals-specific `authorize()` call at all (`apiNames.MEDALS.MANAGE` exists but is never referenced by either route), relying only on generic `Mandate` read access. This is a deliberate "replicate the old app's real behavior" call, not a bug fix — unlike e.g. events' `removeCommentAction`, there's no evidence the missing check was an oversight rather than an intentional "anyone who can see mandates can see medals" design.
- The award-rule arithmetic (`internal/medals/rules.go`) is a line-for-line port of `volunteerMedalSemester`/`committeeMedalSemester`/`gammalOchÄckligSemester` from the old TS, including the "Gammal och Äcklig" `min(7, max(index-of-2nd-board-semester, 5))` calculation and its `vs[7]`-if-fewer-than-2-board-semesters fallback. **A real bug was caught and fixed during implementation, not shipped**: the first version of `ListMemberIDsWithMandateActiveDuring`/`ListMandatesForMembersBefore` used positional `$1`/`$2` SQL params, and the generated Go struct's field order (sqlc names fields by the column each `$N` binds to, not by textual position in the query) didn't match the naive read-top-to-bottom assumption — start/end dates were silently swapped. Caught by testing against real seed-data mandate histories (not just empty/trivial input) and fixed by switching to named `sqlc.arg(...)` params, which make the struct field ↔ SQL role mapping unambiguous. Worth remembering as a general sqlc gotcha: **verify positional-param field ordering against the generated struct, don't assume it, especially with more than one param of the same type** - prefer `sqlc.arg()` whenever a query has 2+ params of the same SQL type.
- `internal/medals.Service.MemberMedals` (a single member's own medals, not just the recipients-by-semester view) is ported and available, but **not wired to any HTTP route in this pass** — its real old-app consumer, `members/[studentId]/+page.server.ts`'s inline medals display, is explicitly out of scope per DESIGN.md's Principles ("doorAccess/medals/ping/phadderGroups/nollaIn aren't part of the directory-foundation port", a documented pre-existing deferral, not something this phase's job to close). Same "exists as a query/method but isn't wired to a route yet" precedent as events' `GetRecurringEvent`.

**Frontend**: `medals/+page.ts` (converted from `+page.server.ts`; `.svelte` stays a stub) calls `GET /medals`, dropping the old load's `sync()` search-index call (unrelated, a later "Search" phase concern per DESIGN.md's roadmap). `medals/download-csv/+server.ts` is a thin proxy to Go's CSV endpoint (forwards the request's cookie header, streams the response back) — no page renders a download link yet, but the endpoint itself is real, matching the "port server logic even without a page.svelte" precedent used throughout this port.

## Auth, scheduling, notifications, uploads

Auth is real, not mocked, by default: `internal/auth.RealAuthenticator` validates an Authentik-issued OIDC session (see DESIGN.md's Auth section for `Identity`, `auth.Require`, policy names in `internal/apinames`, and the full session/refresh design) — `main.go` only falls back to `MockAuthenticator` (a single fixed dev identity with every policy) when `AUTH_MOCK=true` is set explicitly, logged loudly on construction since it must never run against a real deployment. **Scheduling, push notifications, the Discord webhook, and image storage are still mocked, not absent** (for articles - events doesn't use any of these yet, see "Not ported this pass" above) — `internal/integrations` defines real interfaces (`Scheduler`, `Notifier`, `Webhooker`, `Uploader`) called from the real call sites in `articles.Service`, backed only by `Mock*` implementations that log and no-op/return placeholders. See DESIGN.md's "Mocking out-of-scope dependencies" section for the pattern and why nothing about this lives in SvelteKit anymore.

## Consumed by SvelteKit

This section describes the article integration specifically. Events follows the same integration pattern (`+page.ts`/`+page.server.ts`, `$lib/api/client`) for its ported subset — see "Events routes" above for exactly which routes and what's still deliberately left on Prisma.

`../src/lib/api/schema.d.ts` (generated by `openapi-typescript` from this API's own `/openapi.json`) and `../src/lib/api/client.ts` (a thin `openapi-fetch` client, `PUBLIC_GO_BACKEND_URL` from `../.env`) are the only integration surface — **no hand-written client or DTOs** (the earlier `src/lib/server/goApi.ts` and `src/lib/news/getArticles.ts` mapping layer are deleted). Every article component and route imports `components["schemas"][...]` from `schema.d.ts` directly and calls `api.GET/POST/PATCH/DELETE(...)` from `client.ts` at the call site — see DESIGN.md's "API shape and frontend integration" section. `client.ts` lives outside `$lib/server` because SvelteKit's universal `+page.ts` loads call this API directly from the browser as well as the server, per that same section's "every article page moves to `+page.ts`" decision.

Things to know if you're touching either side of this boundary:

- **Locale.** Every translated field ships *both* the raw pair (`headerSv`/`headerEn`) *and* one resolved field (`header`), chosen server-side (`internal/locale`) from the `Accept-Language` header, replicating the old Prisma `translationExtension`'s fallback rule. `client.ts` sets that header from paraglide's `getLocale()` on every request. Display code reads the resolved field; edit forms read the raw pair. Nothing on the frontend computes a resolved value itself.
- **List items don't carry full comments**, only `commentCount` — `ArticleSummary` vs `ArticleDetail` (`ArticleSummary` + `comments`) in the generated schema, unlike the old Prisma version where both queries happened to share one `include` and so had the same shape.
- **Likes/comments are wired up on both sides but unused by the web frontend** — no like button or comment UI exists in `src/routes/(app)/news/` today (confirmed by searching for consumers before assuming otherwise). `Author.type`/full likers list therefore stay minimal here (a `likeCount`, not a per-viewer "liked" boolean or full list) — revisit if a real like button ships.
- **The article feature's own pages use this API's `GET /tags` exclusively**, not `src/lib/news/tags.ts` (the Prisma version that excludes nollning-prefixed tags) - nollning tags show up in the news filter/author-picker forms as a result, which is an accepted trade (see DESIGN.md's mocking principle), not an oversight. `src/lib/news/tags.ts` itself still exists and is still correct to use for the non-article features that share it (Events, member settings) - it just isn't called from anywhere under `src/routes/(app)/news/` anymore.
- **Editing always re-attributes the article to whoever saves the edit** (see the create/update bullet above) — the edit page's author picker is built from the *editor's* own mandates/custom-authors, not the original author's; see DESIGN.md's implementation notes for the full reasoning.

## Repo context

This directory is a subdirectory of the larger `Dsek-LTH/web` git repository (branch `go-backend`), not its own git repo. The repository root (one level up) is a SvelteKit/TypeScript/Prisma/ZenStack project — see `../README.md` and `../AGENTS.md` for that project's context. Do not assume this Go backend shares any of that project's tooling (pnpm, ESLint, Prettier, etc.); it is built and run independently. `go build` output binaries (e.g. `./backend`) are gitignored — don't commit them.
