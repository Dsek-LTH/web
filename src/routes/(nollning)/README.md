/nolla is pre-reveal /nollning is post-reveal

We might want to change it to something like this: /nolla is post-reveal, it
checks the time and if < some time it redirects to /new-student (pre-reveal)
